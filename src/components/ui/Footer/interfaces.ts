import { type ReactNode } from "react";

export interface FooterProps {
  titleSlot: ReactNode;
  linksSlot: ReactNode;
  ctaSlot: ReactNode;
  contactInfoSlot: ReactNode;
  socialButtonsSlot: ReactNode;
}
